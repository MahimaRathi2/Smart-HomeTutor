/**
 * ==========================================
 * SMS UTILITY (DISABLED / NO-OP STUB)
 * ==========================================
 * Email notifications via Nodemailer SMTP serve as the primary notification system.
 */

const sendSMS = async () => {
  return true;
};

module.exports = { sendSMS };
